package com.zhang.effective.article09;

/**
 * @Author zhang
 * @Description 覆盖equals时总要覆盖hashcode
 * @Date 2020-07-07 14:12
 * @Version 1.0
 **/
public class Test {

    // 1.在每个覆盖了equals方法的类中，也必须覆盖了hashcode方法。

    // 2.相等的对象必须具有相等的散列码(hash code)。

    // 3.为不相等的对象产生不相等的散列码。

    // 4.

}
